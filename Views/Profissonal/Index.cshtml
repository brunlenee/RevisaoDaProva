@model IEnumerable<RevisaoDaProva1.Models.Profissonal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.profissaoId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.profissaoId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProfissonalId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProfissonalId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProfissonalId })
        </td>
    </tr>
}

</table>
